# SFE v0.5.0 多 Buffer 工作区实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 SFE 从单文件编辑器升级为轻量多文件工作区，支持多个 buffer、快速打开文件、树面板打开文件不丢失当前编辑状态。

**架构：** 在 `sfe.py` 中新增 `EditorBuffer` 保存每个文件的 `TextBuffer`、路径、滚动、诊断、undo 和自动补全占位符；`EditorApp` 继续暴露 `self.path/self.buffer` 作为当前 buffer 视图，降低对现有编辑和绘制逻辑的影响。列表视图增加 cursor/action，使 `:buffers`、`:files`、`Ctrl-P` 和 tree Enter 都走同一套 buffer 打开/切换逻辑。

**技术栈：** Python 标准库、curses、unittest、现有 Debian 打包脚本。

---

### 任务 1：多 Buffer 行为测试

**文件：**
- 修改：`tests/test_sfe_ui.py`
- 涉及：`sfe.py`

- [ ] **步骤 1：编写失败的测试**

在 `EditorNormalModeTests` 中新增测试：

```python
def test_command_edit_opens_second_buffer_without_discarding_dirty_first(self):
    with tempfile.TemporaryDirectory() as tmp:
        root = Path(tmp)
        first = root / "one.c"
        second = root / "two.c"
        first.write_text("int one;\n", encoding="utf-8")
        second.write_text("int two;\n", encoding="utf-8")
        app = EditorApp(stdscr=None, path=str(first))
        app.mode = "INSERT"
        app._handle_insert_key("x")
        app.mode = "NORMAL"

        app._execute_command(f"e {second}")
        app._execute_command("bp")

    self.assertEqual(app.path, first)
    self.assertEqual(app.buffer.lines[0], "xint one;")
    self.assertTrue(app.buffer.dirty)
    self.assertEqual(len(app.buffers), 2)
```

新增同类测试覆盖：
- 重复打开同一路径切换已有 buffer。
- `:buffers` 展示 `%` 当前标记和 `+` dirty 标记。
- `:bn`、`:bp` 在多个 buffer 间环绕切换。
- `:bd` 拒绝删除 dirty buffer、能删除 clean buffer、删除最后一个 clean buffer 后创建 `[No Name]`。

- [ ] **步骤 2：运行测试验证失败**

运行：

```bash
python -m unittest tests.test_sfe_ui.EditorNormalModeTests
```

预期：新增测试 FAIL，原因是 `EditorApp` 没有 `buffers`，并且当前 `_open_file` 会拒绝 dirty buffer。

### 任务 2：实现 Buffer 状态模型

**文件：**
- 修改：`sfe.py`
- 测试：`tests/test_sfe_ui.py`

- [ ] **步骤 1：新增 `EditorBuffer`**

在 import 后的 dataclass 区域加入：

```python
@dataclass
class EditorBuffer:
    path: Path | None
    buffer: TextBuffer
    row_offset: int = 0
    col_offset: int = 0
    diagnostics: list[BuildDiagnostic] | None = None
    undo: UndoManager | None = None
    auto_pair_placeholders: list[tuple[int, int, str]] | None = None
```

初始化时创建 `self.buffers`、`self.current_buffer_index`，并通过 `_activate_buffer()` 同步 `self.path/self.buffer/self.row_offset/self.col_offset/self.diagnostics/self.undo/self.auto_pair_placeholders`。

- [ ] **步骤 2：实现切换/打开/删除 helper**

新增方法：
- `_current_editor_buffer()`
- `_sync_current_buffer_state()`
- `_activate_buffer(index)`
- `_find_buffer_index(path)`
- `_open_file(path)`：如果已打开则切换；如果当前是 clean unnamed 则复用；否则追加新 buffer；不因当前 dirty 而拒绝。
- `_next_buffer(delta)`
- `_delete_current_buffer()`
- `_show_buffers()`

- [ ] **步骤 3：运行测试验证通过**

运行：

```bash
python -m unittest tests.test_sfe_ui.EditorNormalModeTests
```

预期：新增多 buffer 测试通过，既有 normal mode 测试不回退。

### 任务 3：快速打开和列表选择

**文件：**
- 修改：`tests/test_sfe_ui.py`
- 修改：`sfe.py`

- [ ] **步骤 1：编写失败的测试**

新增测试：

```python
def test_ctrl_p_opens_project_files_list_and_enter_opens_selected_file(self):
    with tempfile.TemporaryDirectory() as tmp:
        root = Path(tmp)
        (root / "Makefile").write_text("all:\n", encoding="utf-8")
        first = root / "main.c"
        second = root / "src.c"
        first.write_text("int main;\n", encoding="utf-8")
        second.write_text("int src;\n", encoding="utf-8")
        app = EditorApp(stdscr=None, path=str(first))

        app._handle_normal_key("\x10")
        app.list_cursor = app.list_lines.index("src.c")
        app._handle_list_key("\n")

    self.assertEqual(app.path, second)
    self.assertEqual(app.mode, "NORMAL")
```

新增测试：
- `:files` 打开文件列表。
- list 中 `j/k` 或方向键移动光标。
- tree Enter 打开文件时 tree 保持 visible，focus 回 editor，buffer 数增加。
- status line 包含 `Buf current/total`。

- [ ] **步骤 2：实现列表动作**

在 `EditorApp` 中新增：
- `self.list_cursor`
- `self.list_row_offset`
- `self.list_actions`
- `_show_project_files()`
- `_open_selected_list_item()`
- `_move_list_cursor(delta)`

`_handle_list_key()` 支持 `j/k`、方向键、Enter。`Ctrl-P` 在 normal/insert 下触发 `_show_project_files()`；`:files` 同样触发。

- [ ] **步骤 3：运行测试验证通过**

运行：

```bash
python -m unittest tests.test_sfe_ui.EditorNormalModeTests tests.test_sfe_ui.EditorLayoutTests
```

预期：快速打开、tree、status 相关测试通过。

### 任务 4：版本与文档

**文件：**
- 修改：`VERSION`
- 修改：`README.md`
- 修改：`packaging/debian/sfe.1`
- 修改：`.github/workflows/release.yml`（仅当发布资产列表需要调整）

- [ ] **步骤 1：更新版本**

把 `VERSION` 改为：

```text
0.5.0
```

- [ ] **步骤 2：更新用户文档**

README 和 man page 增加：
- `:buffers`
- `:bn` / `:bp`
- `:bd`
- `:files`
- `Ctrl-P`
- tree 面板 Enter 打开/切换 buffer
- status 中 `Buf current/total`

- [ ] **步骤 3：运行文档/版本相关测试**

运行：

```bash
python -m unittest discover -s tests
```

预期：全部测试通过。

### 任务 5：完整验证、提交、合并、发布

**文件：**
- 修改：以上所有变更

- [ ] **步骤 1：完整验证**

运行：

```bash
python -m unittest discover -s tests
python -m py_compile sfe.py sfe_core.py scripts/build_deb.py
git diff --check
```

预期：全部 exit 0。

- [ ] **步骤 2：提交 feature 分支**

运行：

```bash
git status --short
git add sfe.py tests/test_sfe_ui.py README.md VERSION packaging/debian/sfe.1 docs/superpowers/plans/2026-05-23-v0.5.0-buffers.md
git commit -m "feat(editor): add multi-buffer workspace"
```

- [ ] **步骤 3：合并到 main 并打 tag**

运行：

```bash
git -C ../.. checkout main
git -C ../.. merge --ff-only feature/v0.5.0-buffers
git -C ../.. tag v0.5.0
git -C ../.. push origin main
git -C ../.. push origin v0.5.0
```

- [ ] **步骤 4：构建和发布 deb**

构建：

```bash
python scripts/build_deb.py
```

如本地无法生成 Linux deb，则上传源码到 `rmer@192.168.1.145:/tmp/sfe-release-v0.5.0` 后在远端 Ubuntu 构建 amd64，并按既有 arm64 runtime 复用策略生成 arm64 包。

发布资产：
- `dist/sfe_0.5.0_amd64.deb`
- `dist/sfe_0.5.0_arm64.deb`
- `dist/sfe_latest_amd64.deb`
- `dist/sfe_latest_arm64.deb`
- `dist/sfe_latest_all.deb`

- [ ] **步骤 5：远端安装验证**

在 `192.168.1.145` 上运行：

```bash
echo rm | sudo -S dpkg -i /tmp/sfe_0.5.0_amd64.deb
sfe --version
for i in 1 2 3 4 5; do /usr/bin/time -f '%e' sfe --version >/dev/null; done
```

预期：版本输出 `sfe 0.5.0`，版本 fast path 仍接近瞬时。

# v0.5.6 Workflow Polish Implementation Plan

> **For AI agents:** implement this plan with TDD in a dedicated git worktree.

**Goal:** make the C build/run loop and common Vim command workflow faster inside SFE.

**Architecture:** keep changes scoped to `sfe.py` command dispatch, list-mode handling, docs, packaging metadata, and UI tests. Reuse the existing `build_runner` abstraction for shell commands and reruns.

**Tech Stack:** Python curses editor, unittest, Debian packaging metadata.

---

## Files

- Modify `sfe.py` for command dispatch and list-mode behavior.
- Modify `tests/test_sfe_ui.py` for regression coverage.
- Modify `README.md`, `packaging/debian/sfe.1`, `.github/workflows/release.yml`, and `VERSION` for release notes.

## Tasks

### Task 1: Build Failure List

- [ ] Add a failing test that `:make` with a nonzero return code opens the `Build Errors` list while preserving a `Build failed` status.
- [ ] Implement the smallest change in `_run_build`.
- [ ] Run the targeted test.

### Task 2: Run Output Rerun

- [ ] Add a failing test that pressing `r` in the `Run Output` list reruns the last run command.
- [ ] Add a status hint for `r rerun` in run output.
- [ ] Run the targeted test.

### Task 3: Vim Command Shortcuts

- [ ] Add failing tests for `:42`, `:pwd`, and `:!cmd`.
- [ ] Extend `_execute_extended_command` with numeric line jumps, project-root display, and shell output list.
- [ ] Run the targeted tests.

### Task 4: Release Update

- [ ] Bump `VERSION` to `0.5.6`.
- [ ] Update README, man page, and release workflow notes.
- [ ] Run full verification: `python -m unittest discover -s tests`, py_compile, and `git diff --check`.

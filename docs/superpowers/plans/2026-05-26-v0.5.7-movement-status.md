# v0.5.7 Movement and Status Implementation Plan

> **For AI agents:** follow TDD. Add failing tests before production changes and keep each edit scoped.

**Goal:** ship the requested Vim movement, safer dirty-buffer quit feedback, clearer status bar, documentation, and `v0.5.7` release assets.

**Architecture:** extend existing `EditorApp` input handling, list-mode helpers, status drawing, and build/run command paths. No new subsystem is needed.

**Stack:** Python curses editor, unittest, Debian packaging workflow.

## Files

- Modify `sfe.py` for movement, quit guard, unsaved list, read-only marker, and timing.
- Modify `tests/test_sfe_ui.py` for behavior coverage.
- Modify `README.md`, `packaging/debian/sfe.1`, `.github/workflows/release.yml`, and `VERSION` for release documentation.

## Tasks

- [ ] Add failing UI tests for `gg`, `G`, `Ctrl-D`, `Ctrl-U`.
- [ ] Add failing UI tests for dirty-buffer quit list and forced quit.
- [ ] Add failing UI tests for `DIRTY`, `RO`, and build/run elapsed-time status.
- [ ] Implement the minimal editor changes to pass the tests.
- [ ] Update documentation and release notes for `v0.5.7`.
- [ ] Run full verification, commit, merge to `main`, tag `v0.5.7`, and publish release assets.
